#include "main.h"
/**
 * main - tests function if integer is positive or negative
 * Return: 0
 */
int main(void)
{
	int i;

	int j;

	int k;

	i = 4;
	j = -6;
	k = 0;
	positive_or_negative(i);

	positive_or_negatice(j);

	positive_or_negative(k);

	return (0);
}
