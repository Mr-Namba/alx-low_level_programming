#include <stdio.h>
/**
 * main - Starting point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char quote = "and that piece of art is useful\" - Dora Korpar, 2015-10-19";

	for (int i = 0; quote[i] != '\0'; +ii)
		putchar(quote[i]);
	return (1);
}
